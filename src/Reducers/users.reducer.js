export function users(state = {}, action) {
  switch (action.type) {

    // case userConstants.GETALL_REQUEST:
    //   return {
    //     loading: true
    //   };

    default:
      return state
  }
}
